export enum eItemType {
  armor,
  weapon,
  container,
  consumable,
  tool
}

export enum eEquipSlotType {
  head = "head",
  neck = "neck",
  leftHand = "leftHand",
  rightHand = "rightHand",
  ring = "ring",
  legs = "legs",
  boots = "boots",
  accessory = "accessory",
  inventory = "inventory"
}

export enum eSkillType {
  magic,
  fist,
  club,
  sword,
  axe,
  distance,
  shield,
  fishing
}

export enum eEffectType {
  restoreLife,
  restoreMana,
  dealDamage
}
